const DeepSeekLogo = ({ className = 'w-5 h-5' }: { className?: string }) => {
  return (
    <img src="/icons/deepseek.svg" alt="DeepSeek" className={className} />
  );
};

export default DeepSeekLogo;
